apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "payforlegalaid-ui.fullname" . }}-test-connection"
  labels:
    {{- include "payforlegalaid-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "payforlegalaid-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
