name: Feature Branch CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a script
        run: echo "This is the first workflow running!"

      - name: Create a dummy artifact
        run: echo "Dummy artifact" > artifact.txt

