name: test2

on:
  push:
    branches:
      - feature/LPG-665-pipeline-to-dev

jobs:
  process:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a script
        run: echo "This is the first workflow running!"